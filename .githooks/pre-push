#!/bin/sh

echo "Running CI checks before push..."
just ci
status=$?

if [ $status -ne 0 ]; then
    echo "❌ CI checks failed. Fix the issues before pushing."
    exit 1
fi

echo "✅ All CI checks passed."
exit 0
